packages:
  mypackage:
    specfile_path: mypackage.spec

packit_instances:
  - prod

targets:
  - fedora-all

jobs:
  - job: copr_build
    trigger: pull_request
    targets:
      - fedora-all
    packages:
      - mypackage
